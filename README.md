# AgriTech Project

## Setup

### Download Data and Model

Install DVC if not already installed:
```bash
pip install dvc
```

Download data and model:
```bash
dvc pull
```

### Run Streamlit UI with Docker

Build (1st time only) and run the Docker image:
```bash
docker compose up
```
